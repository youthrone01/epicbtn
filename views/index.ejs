<html>
<head>
    
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script type ="text/javascript" src="/socket.io/socket.io.js"></script>
    <script type ="text/javascript">
        $(document). ready(function (){
        // this triggers the connection event in our server!
        var socket  = io.connect();
        // we'll write all the socket stuff after the above line!
        $("#update").click(function(){
            socket.emit("click_btn",{res:"one more"});
        })

        $("#reset").click(function(){
        socket.emit("reset",{res:"reset"});

        })

        socket.on("update",function(data){
            $('span').text(data.res);
        });
        })
    </script>
</head>

<h1>The button has been pushed <span>0</span> time(s)</h1>
<h3>Push the button to update the count</h3>
<button id="update">Push the epic button</button><br>
<button id="reset">Reset count</button>


</html>